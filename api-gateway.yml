server:
  port: 8765

zuul:
  prefix: /api
  routes:
    user-webservice: 
      path: /user/**
      stripPrefix: false

      
   

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
