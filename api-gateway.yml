server:
  port: 8765

zuul:
  prefix: /api
  routes:
    user-webservice: /user/**
    task-webservice: /task/**
    auth-server: /auth/**


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
