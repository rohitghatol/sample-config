server:
  port: 8765

zuul:
  routes:
    user-webservice:
      path: /user/**
      stripPrefix: false
    web-portal:
      path: /**
      stripPrefix: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
