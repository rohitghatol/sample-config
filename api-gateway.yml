server:
  port: 8765

zuul:
  routes:
    user-webservice: /api/**
    task-webservice: /api/**
    auth-server: /auth/**
    web-portal: /**

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
