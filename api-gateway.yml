server:
  port: 8765

zuul:
  routes:
  
    user-webservice: /api/users/**
    web-portal: /ui/**

eureka:
  instance:
    leaseRenewalIntervalInSeconds: 10
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
