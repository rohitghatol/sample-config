server:
  port: 8765

zuul:
  prefix: api
  routes:
    web-portal: 
      path: /**
      stripPrefix: false
    user-webservice: /user/**
    task-webservice: /task/**
    auth-server: /oauth/**
   

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
