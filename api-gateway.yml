server:
  port: 8765

zuul:
  routes:
    user-webservice:
      path: /api/user/**
      id: user-webservice
      stripPrefix: false
    web-portal:
      path: /**
      id: web-portal
      stripPrefix: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
